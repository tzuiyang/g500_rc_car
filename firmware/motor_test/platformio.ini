[env:nanoatmega328]
platform  = atmelavr
board     = nanoatmega328
framework = arduino
monitor_speed = 115200

; If your Nano uses the newer bootloader (post-2018), use this instead:
; board = nanoatmega328new

; Auto-detect upload port — or set manually, e.g.:
; upload_port = /dev/ttyUSB0   (Linux/RPi)
; upload_port = COM3           (Windows)
